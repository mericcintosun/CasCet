# deps
node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
target/
*.wasm
# ...except the third-party reference token NOTICE attributes (needed for interop)
!contracts/external/*.wasm

# env & secrets
.env
.env.*
!.env.example
!.env.sample
*.pem
keys/

# data
*.sqlite
*.sqlite-journal
.cascet/

# misc
.DS_Store
*.log
coverage/
.turbo/
contracts/target/

# ts build cache
*.tsbuildinfo
.vercel

# Large media (demo + pitch are published on YouTube; keep them out of git).
# Root-anchored so app assets under apps/*/public are unaffected.
/*.mp4
/*.mov
/*.webm
/*.opus
/*.mp3
/*.wav
/*.m4a
